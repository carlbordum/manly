language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
addons:
  apt:
    update: true
    packages:
      - python3-pip
before_install:
  - sudo pip3 install flit
install:
  - 'flit install --deps all --python "$(which python)"'
script:
  - manly --version
  - py.test
  - manly ls -lah
